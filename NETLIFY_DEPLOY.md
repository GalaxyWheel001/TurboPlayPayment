# 🚀 Инструкция по деплою на Netlify и настройке Telegram Mini App

## 📋 Шаг 1: Подготовка проекта

### 1.1 Проверьте наличие файлов:
- ✅ `netlify.toml` - конфигурация Netlify
- ✅ `netlify/functions/` - Netlify функции
- ✅ `public/` - статические файлы
- ✅ `package.json` - зависимости проекта

### 1.2 Убедитесь, что проект закоммичен в Git:
```bash
git add .
git commit -m "Ready for Netlify deployment"
git push
```

---

## 🌐 Шаг 2: Деплой на Netlify

### Вариант A: Деплой через Netlify UI (рекомендуется)

1. **Зайдите на [netlify.com](https://www.netlify.com)**
   - Войдите или создайте аккаунт

2. **Создайте новый сайт:**
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите ваш Git провайдер (GitHub, GitLab, Bitbucket)
   - Выберите репозиторий с проектом

3. **Настройте параметры сборки:**
   - **Build command:** `npm install` (или оставьте пустым)
   - **Publish directory:** `public`
   - **Branch to deploy:** `main` (или ваша основная ветка)

4. **Добавьте переменные окружения:**
   - Перейдите в "Site settings" → "Environment variables"
   - Добавьте следующие переменные (если нужно):
     ```
     MOONPAY_API_KEY=your_moonpay_api_key_here
     ALLOWED_ORIGIN=https://your-site.netlify.app
     ALLOWED_HOST=your-site.netlify.app
     ```

5. **Деплой:**
   - Нажмите "Deploy site"
   - Дождитесь завершения деплоя
   - Скопируйте URL вашего сайта (например: `https://your-site.netlify.app`)

### Вариант B: Деплой через Netlify CLI

1. **Установите Netlify CLI:**
```bash
npm install -g netlify-cli
```

2. **Войдите в Netlify:**
```bash
netlify login
```

3. **Инициализируйте сайт:**
```bash
netlify init
```
   - Выберите "Create & configure a new site"
   - Выберите название сайта или оставьте автоматическое
   - Выберите команду сборки: `npm install` (или Enter)
   - Выберите директорию публикации: `public`

4. **Добавьте переменные окружения:**
```bash
netlify env:set MOONPAY_API_KEY "your_moonpay_api_key_here"
netlify env:set ALLOWED_ORIGIN "https://your-site.netlify.app"
```

5. **Деплой в production:**
```bash
netlify deploy --prod
```

---

## 🤖 Шаг 3: Настройка Telegram Mini App через BotFather

### 3.1 Создание Mini App

1. **Откройте [@BotFather](https://t.me/botfather) в Telegram**

2. **Создайте новое Mini App:**
   ```
   /newapp
   ```

3. **Выберите вашего бота:**
   - Выберите бота из списка, для которого создаете Mini App

4. **Заполните информацию о Mini App:**
   - **Title:** `Payment Gateway` (или любое другое название)
   - **Short name:** `payment` (или другое короткое имя)
   - **Description:** `Secure payment gateway for Telegram` (или другое описание)
   - **Photo:** Загрузите иконку (рекомендуется 512x512 или 192x192)
   - **Web App URL:** `https://your-site.netlify.app/payment.html`
     - ⚠️ **ВАЖНО:** Замените `your-site` на ваш реальный домен Netlify
   - **Animation:** (опционально) Загрузите GIF анимацию

5. **Сохраните информацию:**
   - BotFather даст вам ссылку на Mini App
   - Скопируйте и сохраните эту информацию

### 3.2 Альтернативный способ - редактирование существующего Mini App

Если у вас уже есть Mini App, используйте:
```
/editapp
```

---

## 🎯 Шаг 4: Интеграция Mini App в бота

### 4.1 Создание кнопки для Mini App

В коде вашего Telegram бота создайте кнопку с Web App:

**Python (python-telegram-bot):**
```python
from telegram import InlineKeyboardButton, InlineKeyboardMarkup

keyboard = [
    [InlineKeyboardButton(
        "💳 Pay Now",
        web_app=WebAppInfo(url="https://your-site.netlify.app/payment.html")
    )]
]
reply_markup = InlineKeyboardMarkup(keyboard)
```

**JavaScript (node-telegram-bot-api):**
```javascript
const { Markup } = require('telegraf');

const keyboard = Markup.inlineKeyboard([
    Markup.button.webApp('💳 Pay Now', 'https://your-site.netlify.app/payment.html')
]);
```

**TypeScript (grammY):**
```typescript
import { InlineKeyboard } from "grammy";

const keyboard = new InlineKeyboard()
  .webApp("💳 Pay Now", "https://your-site.netlify.app/payment.html");
```

### 4.2 Использование в сообщении

```python
# Пример отправки сообщения с кнопкой
await context.bot.send_message(
    chat_id=update.effective_chat.id,
    text="Click the button below to make a payment:",
    reply_markup=keyboard
)
```

---

## ✅ Шаг 5: Проверка работы

### 5.1 Проверьте endpoints:

1. **Health check:**
   ```
   https://your-site.netlify.app/health
   ```
   Должен вернуть: `{"status":"ok"}`

2. **Providers API:**
   ```
   https://your-site.netlify.app/api/providers
   ```
   Должен вернуть список провайдеров

3. **Payment page:**
   ```
   https://your-site.netlify.app/payment.html
   ```
   Должна открыться страница оплаты

### 5.2 Тестирование Mini App:

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку с Mini App
3. Убедитесь, что Mini App открывается
4. Проверьте работу формы оплаты

---

## 🔧 Шаг 6: Настройка домена (опционально)

Если вы хотите использовать свой домен:

1. В Netlify Dashboard перейдите в "Domain settings"
2. Нажмите "Add custom domain"
3. Введите ваш домен
4. Настройте DNS записи согласно инструкциям Netlify

---

## 📝 Важные замечания

### Безопасность:
- ✅ Всегда используйте HTTPS (Netlify предоставляет бесплатный SSL)
- ✅ Не храните API ключи в коде
- ✅ Используйте переменные окружения для секретов

### CORS:
- Если у вас проблемы с CORS, добавьте ваш домен в `ALLOWED_ORIGIN`
- Netlify Functions автоматически обрабатывают CORS

### Обновление:
- После каждого push в основную ветку Netlify автоматически передеплоит сайт
- Или используйте: `netlify deploy --prod`

---

## 🆘 Troubleshooting

### Проблема: Mini App не открывается
- ✅ Проверьте, что URL начинается с `https://`
- ✅ Убедитесь, что домен правильный
- ✅ Проверьте, что сайт задеплоен на Netlify

### Проблема: API endpoints не работают (404)
- ✅ Проверьте `netlify.toml` и правильность redirects
- ✅ Убедитесь, что функции находятся в `netlify/functions/`
- ✅ Проверьте логи в Netlify Dashboard → Functions → Logs

### Проблема: CORS ошибки
- ✅ Добавьте ваш домен в `ALLOWED_ORIGIN`
- ✅ Проверьте headers в Netlify Functions

### Проблема: Функции не работают
- ✅ Проверьте логи в Netlify Dashboard
- ✅ Убедитесь, что все зависимости установлены
- ✅ Проверьте, что Node.js версия совместима (18 в `netlify.toml`)

---

## 📞 Дополнительная помощь

- [Netlify Documentation](https://docs.netlify.com/)
- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [BotFather Guide](https://core.telegram.org/bots/tutorial)

---

**Успешного деплоя! 🎉**

